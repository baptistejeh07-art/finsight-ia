# =============================================================================
# FinSight IA — Dépendances Streamlit Community Cloud
# pip install -r requirements.txt
# Cache-bust: 2026-04-13-23h-v2 (force rebuild SCC apres rename render_running)
#
# EXCLUS intentionnellement (voir requirements-local.txt pour dev complet) :
#   - torch / transformers  : 700MB+, OOM sur free tier SCC (1GB RAM)
#                             AgentSentiment utilise le fallback Finnhub news
#   - ollama                : local uniquement
#   - redis                 : nécessite un serveur externe
#   - chromadb              : base vectorielle locale (Phase 9 Gouvernance)
#   - polars                : pandas suffit en prod
#   - supabase              : fallback JSON local actif
#   - polygon-api-client    : non utilisé en prod
#   - alpha-vantage         : non utilisé en prod
# =============================================================================

# --- LLM Providers ---
anthropic>=0.40.0
groq>=0.9.0
mistralai>=2.0.0
cerebras-cloud-sdk>=1.0.0
google-genai>=0.8.0
openai>=1.50.0

# --- Données financières ---
yfinance>=0.2.40
finnhub-python>=2.4.19

# --- Excel / Rapports ---
openpyxl>=3.1.2
python-pptx>=1.0.0
pdfplumber>=0.10.0
reportlab>=4.0.0

# --- Calcul / Data ---
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0

# --- Interface ---
streamlit>=1.36.0
extra-streamlit-components>=0.1.80   # cookies persistants pour session auth

# --- Auth ---
# supabase fallback JSON local possible mais auth-only mode requiert le client
supabase>=2.5.0

# --- LangGraph ---
langgraph>=0.2.0
langchain-core>=0.1.0

# --- Utils ---
feedparser>=6.0.11
vaderSentiment>=3.3.2
httpx>=0.27.0
python-dotenv>=1.0.0
requests>=2.32.0
scipy>=1.11.0

# --- Backend FastAPI (Railway deployment) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
PyJWT>=2.8.0
cryptography>=41.0.0
